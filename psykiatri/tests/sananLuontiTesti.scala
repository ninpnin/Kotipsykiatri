package tests
import dictionary.{Kotus52, wordInflector}
//import org.junit.Test
//import org.junit.Assert._
import convo.Word

class sananLuontiTesti extends App {
/*
  @Test def testi1a() {
    val a = new Sana("ostaa",1)
    assertEquals("value at (0,0)", "ostan", a.teksti())
  }
  
  @Test def testi1b() {
    val a = new Sana("Pilkku",8)
    assertEquals("value at (0,0)", ",", a.teksti())
  }
  
  @Test def testi1c() {
    val a = new Sana("Kysymysmerkki",5)
    assertEquals("value at (0,0)", "?", a.teksti())
  }
  
  @Test def testi1d() {
    val a = new Sana("olla",1)
    assertEquals("value at (0,0)", "olen", a.teksti())
  }
  */
}